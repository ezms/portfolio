import styled from 'styled-components';

export const MyServicesSectionContainer = styled.section`
    display: flex;
    justify-content: space-evenly;
    align-items: center;
    flex-wrap: wrap;
    gap: 30px;

    height: 100%;
    padding: 3em 0;
    background-color: var(--black-color);
`;
