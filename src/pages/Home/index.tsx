import PresentationSection from '../../components/PresentationSection';

const Home = () => {
    return (
        <div>
            <PresentationSection />
        </div>
    );
};

export default Home;
